node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Anchored to repo root — an unanchored `data/` here was also matching
# src/data/, which silently untracked src/data/clinicalData.ts on every
# fresh clone. That's the actual root cause behind the repeated "restore
# clinicalData.ts" commits in this repo's history.
/data/
uploads/